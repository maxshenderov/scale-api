
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Заголовок") И ЗначениеЗаполнено(Параметры.Заголовок) Тогда
		//Элементы.ДекорацияЗаголовок.Заголовок = Параметры.Заголовок + Символы.ПС + Элементы.ДекорацияЗаголовок.Заголовок;
		Элементы.ДекорацияЗаголовок.Заголовок = Параметры.Заголовок;
		//Сообщить(Параметры.Заголовок);
	КонецЕсли;
	
КонецПроцедуры
