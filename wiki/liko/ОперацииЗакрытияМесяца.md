# ОперацииЗакрытияМесяца

> Обработка закрытия месяца — ранее переопределяла расчет этапов, сейчас весь BSL-код закомментирован.

## Что делает / Доработки
Модуль менеджера содержит закомментированное переопределение `ВыполнитьРасчетЭтапов` (через `&Вместо`). Логика была отключена Маковеевой А.Ю. 03.07.2024. Ранее:
- Пропускала этап «Формирование движений по расчетам с партнерами и переоценка расчетов»
- Добавляла проверку предварительного закрытия месяца

В текущем состоянии модуль не несет активной функциональности — весь код в комментариях.

## Ключевые процедуры
- `Лико_ВыполнитьРасчетЭтапов()` — закомментированное `&Вместо` переопределение (222 строки комментариев)

## Связи (нет активных, код отключен)
