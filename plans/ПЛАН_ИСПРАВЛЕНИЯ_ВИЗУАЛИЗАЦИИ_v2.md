# План исправлений визуализации склада v2

## Данные стеллажа Ряд 301 (из 1С)

| Этаж | Высота мм | Ширина мм | Глубина мм | Вес кг | ratio H/W |
|------|-----------|-----------|------------|--------|-----------|
| 1 (нижний) | 2 186 | 2 700 | 1 100 | ∞ | 0.81 |
| 2 (верхний) | 2 400 | 2 700 | 1 100 | 4 000 | 0.89 |

Коэффициент масштаба: 0.05

---

## Итоговый чеклист для реализации (10 пунктов)

### CSS

- [ ] **Рамка sections-row**: Добавить `width: max-content` для `.sections-row` чтобы рамка охватывала все секции при масштабе
- [ ] **CSS height-info**: Исправить позиционирование — `position: absolute; left: -55px; top: 50%; transform: translateY(-50%); writing-mode: vertical-rl; text-orientation: mixed;`
- [ ] **Метка этажа влево**: Переместить `.floor-header` с блочного положения над секциями в абсолютную позицию слева (рядом с height-info). Или: вынести "Этаж X" и "Qb = Y кг" в абсолютный блок `left: -55px` как height-info, но под height-info
- [ ] **Убрать расстояние между этажами**: `.floor { margin-bottom: 0 }` или `margin-bottom: 2px` (было 10px)
- [ ] **Цвета текста**: Все серые надписи сделать чёрным:
  - `.section-name` — `color: #000` (было #888)
  - `.cell-name` — `color: #333` (было #aaa)
  - `.weight-remaining` — `color: #000` (было #999)
  - `.pallet-size` — `color: #333` (было #555)
  - `.floor-width` — `color: #333` (было #666)
- [ ] **Цвет паллета**: Сменить с розового на светло-жёлтый с серой рамкой:
  - `.pallet-occupied` — `background: rgba(255,255,200,0.7); border: 2px solid #888;`
  - `.pallet-reserved` — `background: rgba(255,255,220,0.5); border: 2px dashed #888;`

### HTML генерация

- [ ] **Ширина под первым этажом**: В строке 688 заменить `ПервыйЭтажНомер = 0` + `>` на `ПервыйЭтажНомер = 99999` + `<` — показывать ширину только под нижним этажом
- [ ] **Свободное пространство**: Переместить текст `free-space` с `bottom: 16px` на `top: 16px` (над паллетом), чтобы не перекрывал размер паллета
- [ ] **Остаточный вес**: Переместить `weight-remaining` из абсолютной позиции `bottom: 2px` в верхнюю часть секции с `top: 28px` (ниже section-name)
- [ ] **CSS min-height**: Добавить `min-height` для sections-row чтобы этажи с весогрузкой 0 тоже были видны

---

## Схема расположения элементов

```
[height-info 2400мм] [Этаж 2 Qb=4000кг]  ← метки этажа слева
┌──────────────────────────────────────────┐
│ section-name: P301-M(1-2-3)-Э2          │
│ cell-name: P301M1Э2  P301M2Э2  P301M3Э2 │
│ ┌──────────┐                             │
│ │ П34      │  ← П34                      │
│ │← 900мм | 900мм →│  ← свободное пространство (над паллетом)
│ │ 1600×900 │  ← размер паллета           │
│ └──────────┘                             │
│ ost: 4000 кг  ← остаточный вес           │
└──────────────────────────────────────────┘
         2700 мм  ← ширина (только под нижним этажом)
```

---

## Зависимости

- Все изменения в файле [`1c/ERP/obrab/ZagSklad/ЗагрузкаСклада/Forms/Форма/Ext/Form/Module.bsl`](1c/ERP/obrab/ZagSklad/ЗагрузкаСклада/Forms/Форма/Ext/Form/Module.bsl)
- CSS блок: строки ~437-481
- HTML генерация: строки ~488-705
